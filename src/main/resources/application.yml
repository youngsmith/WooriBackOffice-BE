spring:
  profiles:
    active: local # 기본 환경 선택

---
logging.level.org.hibernate.SQL: DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  config:
    activate:
      on-profile: local

  datasource:
    hikari:
      username: manager
      password: duddus186213!
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/woori

  flyway:
    enabled: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 511913710332-rs8ep41966gsco2gh8ebelsk8ojfsaoh.apps.googleusercontent.com
            client-secret: YgpTiOOg4kdbKi2aS6l1U3Gt


